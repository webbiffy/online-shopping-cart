import styled from 'styled-components';

export const Title = styled.h1`
    font-size: 1.5em;
    color: white;
    padding: .5em;
    margin: 0;
`;

export const Wrapper = styled.section`
    background: #505050;
`;